name: job

services:
  nginx:
    image: nginx:alpine
    ports:
      - 80:80
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - backend

  backend:
    build:
      context: ../
      dockerfile: docker/backend/Dockerfile.dev
    volumes:
      - ../backend:/app
